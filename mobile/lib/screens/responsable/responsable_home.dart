import 'package:flutter/material.dart';

class ResponsableHome extends StatelessWidget {
  const ResponsableHome({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("🏪 Page Responsable"));
  }
}
